# MoodTube 버전 관리 문서 (CHANGELOG)

MoodTube 프로젝트의 최초 알파 빌드부터 구글 플레이스토어 배포 및 최신 성능 최적화 패치까지의 모든 버전 릴리즈 이력을 기록하는 문서입니다.

---

## [0.3.3+5] - 2026-07-05 (프론트엔드 품질 대개편 — 죽은 카탈로그 청산 + 모션 도입)
### 추가 및 변경 사항
*   **죽은 영상 ID 전면 청산 (콘텐츠 신뢰 회복)**: 카탈로그의 43개 영상 ID를 YouTube oEmbed로 전수 검증한 결과 20개가 삭제/비공개 상태(회색 썸네일·재생 불가의 원인)였습니다. 전부 실존 검증된 영상(Lofi Girl 라디오, HALIDONMUSIC Best of Mozart, Chillhop Radio, The Good Life Radio 등)으로 교체하고 실제 제목/채널명으로 표기를 정정했습니다. 정기 검증용 `tools/check_catalog.sh`(oEmbed 200 + 썸네일 존재 검사)를 신설했습니다.
*   **재생수 표기 정리**: `8500.0만회` 같은 무의미한 `.0` 소수점을 제거했습니다(100 미만 스케일 값만 소수 1자리 유지). 한국어/중국어/영어 포맷 공통 적용(`AppText._scaled`).
*   **무드 다이얼 폴리시**: 선택 무드 라벨이 노드 밑과 재생 버튼 밑에 중복 표시되던 것을 노드 라벨 하나로 정리했고, 게이지 호와 노브가 `TweenAnimationBuilder`(260ms, easeOutCubic)로 부드럽게 이동하도록 모션을 넣었습니다.
*   **스마트 픽 카드 정돈**: 부제에 내부 검색 쿼리가 날것으로 노출되던 것을 `'{무드명}' 무드 기준 추천` 문구로 가공했고, 스포트라이트 카드는 배지 옆 태그를 1개로 제한 + 채널명 태그 중복 제거했습니다. 배지 폰트를 9.5→12로 올려 디자인 시스템 최소 글자 규칙을 준수합니다.
*   **무드 그리드 & Scapetune 배너**: 그리드 카드에 무드 설명 한 줄을 추가해 비어 보이던 중앙을 채우고(비율 1.48→1.22), 홀수 슬롯이라 마지막 줄에 외톨이로 남던 `_ScapetuneGridCard`를 그리드 아래 **전폭 배너**(`_ScapetuneBanner`: 썸네일 + '오늘의 채널 픽' 아이브로우 + 스펙트럼 재생 버튼)로 전환했습니다.
*   **시그니처 모션 — 무드가 화면을 물들임**: 홈 다이얼에서 무드를 선택하면 `SoftBackdrop` 하단 글로우가 해당 무드의 액센트 색으로 700ms에 걸쳐 서서히 물듭니다(`SoftPage.tint`).
*   **화면 전환/광고 모션**: 하단 탭 전환에 220ms 크로스페이드(`AnimatedSwitcher`)를 적용했고, 네이티브 광고 로드 시 110px가 한 프레임에 끼어들며 리스트가 튀던 것을 `AnimatedSize`(300ms)로 부드럽게 펼쳐지게 했습니다.
*   **화면별 폴리시**: 탐색 부제를 한 줄 카피로 교체("기분을 검색해 딱 맞는 음악을 찾아보세요."), 보관함 카테고리 칩 행에 우측 페이드로 스크롤 힌트 추가, 설정 화면을 인터랙티브 항목(언어/테마/차단 초기화) 우선으로 재배치하고 초기화를 `OutlinedButton`으로 교체해 버튼 어포던스를 살렸습니다.
*   **AdMob 최종 승인 및 연동 완료**: 구글 플레이 스토어 출시 이후 AdMob 콘솔에서 앱 연동, 앱 인증(`확인됨`) 및 승인 상태(`준비됨`)를 모두 획득하여 온전한 광고 수익화 환경 조성을 완결했습니다.
*   **검증**: `flutter analyze` 경고 0(스타일 info만), `flutter test` 8개 전원 통과(카탈로그 실제 제목 전환에 맞춰 순위 테스트를 videoId 기반으로 보강), 웹 미리보기 라이트/다크 전 화면 스크린샷 확인 완료.

---

## [0.3.2+4] - 2026-07-05 (안정성 강화 및 다국어 완비 버그 릴리즈)
### 추가 및 변경 사항
*   **미니플레이어 이중 재생 및 오디오 겹침 해결**: 검색 결과 화면(`ResultsScreen`) 내 미니플레이어 UI 및 불필요한 컨트롤러 관리를 영구 제거하여, 소리가 중복 재생되거나 시스템 오디오 리소스 락이 걸리는 문제를 원천 차단했습니다.
*   **Flutter 빌드 생명주기 안정화**: 미니플레이어 동기화 컨트롤러의 해제(`dispose()`) 과정을 `Future.microtask`를 활용해 비동기 포스트프레임으로 격리시켜, 빌드 루프 수행 중 리소스가 파괴되는 Flutter 규칙 위반 에러를 완전히 해결했습니다.
*   **다이얼 테마 전환 실시간 동기화**: 감정 무드 다이얼 화가(`_AuroraDialPainter`)에 `isDark` 상태값을 직접 바인딩하고 `shouldRepaint`에서 이를 함께 비교하도록 리팩토링하여, 다크/라이트 모드 전환 시 다이얼 컴포넌트가 실시간으로 완벽히 리페인팅되도록 수정했습니다.
*   **다국어(i18n) 자체 개발 규격 완수**:
    *   업로드일 라벨 하드코딩 제거 및 `AppText.published` 파서에 `'업로드일 정보 없음'`의 다국어 번역(`No upload date`, `无上传日期`) 매핑을 완료했습니다.
    *   플레이어 오버레이 버튼의 하드코딩을 제거하고 `text.openInYouTube` 게터로 바인딩했습니다.
    *   보관함 빈 상태의 인라인 삼항연산자 분기 텍스트를 `text.libraryEmptySubtitle` 로 캡슐화 이관했습니다.
*   **API 호출 실패 시 일일 쿼터 롤백**: YouTube API 호출이 예기치 못하게 실패(403 권한 해제 포함)하거나 빈 결과가 유입될 때 쿼터를 헛되이 낭비하지 않고 차감했던 카운트를 복구하는 `rollbackApiCall()` 보정 장치를 탑재했습니다.
*   **트래킹 데이터 누적 한도(50개) 가비지 컬렉터 도입**: 노출 및 클릭 트래킹 데이터가 무한히 쌓여 로컬 DB 용량을 잠식하는 것을 방지하기 위해, 저장 시점에 노출일이 가장 오래된 데이터부터 자동으로 삭제하여 최대 50개 데이터만 유지하는 슬라이딩 윈도우 GC를 구현했습니다.
*   **홈 스마트픽 썸네일 캐싱 적용**: 홈 화면의 스마트 픽 위젯(`SmartPickCard`) 썸네일 로딩부를 `Image.network`에서 `CachedNetworkImage`로 교체하여 불필요한 재로딩 및 프레임 드랍을 해소했습니다.
*   **광고 템플릿 테마 동기화**: Native Ad 로딩 시 라이트/다크 테마 판단 기준을 기기 시스템 밝기에서 앱 내부 테마 설정인 `DesignTokens.isDark`에 직접 정렬시켜 눈부심 및 테마 불일치를 해결했습니다.
*   **README.md 아키텍처 최적화**: 모듈화 완료 시점의 낡은 설명을 지우고, 기본 API 우선 모드(`apiMode: true`) 기술 불일치와 기능별 16개 독립 모듈 아키텍처 설명을 공식 업데이트했습니다.

---

## [0.1.3] - 2026-06-29 (프로덕션 배포용 빌드 - 최신화)
### 추가 및 변경 사항
*   **A/B 테스트 배지 및 다국어 지원**: 추천 채널 배지의 A/B 테스트 검증을 위해 A안('추천 채널')과 B안('이 분위기엔 이 채널')의 번역 리소스를 탑재하고 노출 분기 처리를 완료했습니다.
*   **스포트라이트 임프레션/클릭 지표 트래킹 연동**: 스마트 픽 내 Scapetune 등 스포트라이트 추천 곡의 기기 노출 시 `trackImpression`, 재생 시 `trackClick`을 동적으로 호출하여 마케팅 통계 지표를 수집할 수 있도록 추적 코드를 추가했습니다.
*   **그리드 꼬리 큐레이션 전용 카드 신설**: '무드로 둘러보기' 그리드 맨 끝 슬롯에 브랜드 스펙트럼 데코레이션을 입힌 `_ScapetuneGridCard` 위젯을 신설 배치하여, 터치 시 당일의 추천 큐레이션 플레이어 화면으로 바로 진입할 수 있는 스페셜 루트를 구축했습니다.
*   **추천 스코어링 알고리즘 고도화**: 스포트라이트 비디오 추천 시 기존 고정 `+30점` 가중치 대신, 해당 무드 키워드와 추천 비디오 태그의 겹침 수에 따라 추가 보너스 점수를 비례해서 지급하는 동적 가중치 공식(`15 + matchBonus * 5`)으로 업그레이드했습니다.
*   **main.dart 구조 모듈화 (C1)**: 5,145줄 of 단일 소스 파일을 16개의 독립된 기능별 모듈(state, services, widgets, screens 등)로 완전히 분리하여 가독성 및 유지보수성을 극대화했습니다.
*   **API 재시도 로직 보강 (W5)**: 네트워크 일시 오류나 API Rate Limit(429), 5xx transient 에러 시 최대 3회까지 지수 백오프 대기 시간(Exponential Backoff)을 두고 자동 재시도하는 `_retryGet` 비동기 헬퍼를 도입하여 네트워크 불안정 대응력을 대폭 강화했습니다.
*   **MaterialApp 렌더링 최적화 (W9)**: 기존 `Consumer`에서 Dart 3.0 레코드 구조 분해 문법을 활용한 `Selector<MoodTubeState, (ThemeMode, Locale?)>`로 리팩토링했습니다. 음악 재생 시간, 볼륨 등 무관한 상태가 변할 때 `MaterialApp` 전체가 통째로 빌드되어 Jank(프레임 렉)를 일으키던 낭비를 완벽히 차단했습니다.
*   **미니플레이어 메모리 누수 및 레이스컨디션 해결 (W12)**:
    *   재생이 정지되어 하단 미니플레이어가 사라질 때, 기존 컨트롤러 인스턴스(`miniController`)를 즉각 `dispose`하고 null로 초기화해 메모리 누수 및 오디오 중복 재생 오류를 차단했습니다.
    *   비동기 채널 상태 이벤트 리스너 내부에서 `context`를 참조하기 전 `!mounted` 체크를 보강하여 화면 이동 시 발생하는 unmounted context 크래시를 원천 예방했습니다.
*   **내부 카탈로그(mockCatalog) 다양화 (06-26)**: Lofi Girl, Classic FM, Nature Ambience 등에서 임베드 제한이 없는 8개의 실존 고품질 플레이리스트 비디오 ID를 mockCatalog 각 장르 카테고리에 분배 이식하여 오프라인/API 한도 초과 시에도 풍성한 큐레이션을 즐길 수 있도록 보강했습니다.
*   **이미지 캐싱 보강 (C3)**: `cached_network_image` 패키지를 연동하고 기존 `Image.network`를 `CachedNetworkImage`로 일괄 대체하여 썸네일 재로딩 프레임 지연을 해소했습니다.
*   **전역 에러 핸들러 (C4)**: `FlutterError.onError`와 `PlatformDispatcher.instance.onError`를 진입점에 바인딩하여 예기치 못한 네이티브/플러터단 런타임 오류 시 크래시(앱 다운) 없이 전역 로깅하도록 개선했습니다.
*   **PlayerScreen 컨텍스트 릭 수정 (C2)**: 플레이어 화면이 dispose되는 시점의 비동기 안전성을 위해 `initState` 단계에서 `MoodTubeState`를 미리 캐싱해두어 BuildContext leak을 방지했습니다.

---

## [0.1.2] - 2026-06-21 (최초 플레이 스토어 배포 버전)
### 추가 및 변경 사항
*   **유튜브 재생 불가 영상 자동 차단 (블랙리스트)**: 임베드 차단이나 지역 제한으로 재생 시점 오류를 내뿜는 비디오 ID를 동적으로 포착하여 로컬 블랙리스트(`SharedPreferences`)에 영속 등록하고, 향후 추천/검색 결과 UI 노출에서 원천 제외 및 다음 곡 자동 스킵을 지원하도록 조치했습니다.
*   **무드 다이얼 게이지 렉 해결**: 매 드래그 프레임마다 전체 화면이 리빌드되어 UX를 저해하던 현상을 `AuroraMoodDial` 내부 로컬 상태 격리 및 드래그 종료 시점(`onPanEnd`, `onTapUp`) 1회 전파 방식으로 최적화하여 60FPS UX를 확보했습니다.
*   **바텀 시트 다이얼 레이아웃 비율 고정**: 기기 화면 폭이 좁거나 바텀 패딩 축소 시 렌더링이 찌그러지던 현상을 `FittedBox` 비율 스케일다운을 적용하여 완벽하게 방어하고, 메인 화면 다이얼과 헤더 스타일을 통일했습니다.
*   **카테고리/검색 결과 뒤로가기 재생 연동**: `ResultsScreen`에서 재생 중인 플레이어를 닫고 뒤로 나올 때 소리가 끊기던 문제를 해결하기 위해 결과 화면 내에 `MiniPlayer` 생명주기를 완벽 연동했습니다.
*   **적응형 앱 아이콘 & 오로라 스플래시**: 다이얼-게이지 모티프의 전경 레이어와 라벤더 핑크 배경을 적용한 Android 12+ 적응형 아이콘(`ic_launcher_foreground.png` 등) 및 라이트/다크 모드에 모두 반응하는 오로라 스플래시 화면을 최종 팩킹했습니다.

---

## [0.1.1] - 2026-06-12 (상용 빌드 및 마켓 준비 버전)
### 추가 및 변경 사항
*   **정식 Application ID 적용**: 패키지 식별자를 `com.moodtube.app`으로 전면 변경하고 Android 네이티브 디렉토리 구조와 `MainActivity.kt` 파일을 정밀하게 이관했습니다.
*   **릴리즈 서명 설정**: 릴리즈 키스토어(`upload-keystore.jks`) 및 비밀 설정 파일(`key.properties`) 구조를 구성하여 빌드 오토메이션을 적용했습니다.
*   **최근 검색어 기능 구현**: SharedPreferences를 통해 각 국어별 최근 검색어를 영속 저장하고, 칩(Chip) UI를 활용한 이력 조회 및 개별 삭제 기능을 추가했습니다.
*   **햅틱 피드백 미세조정**: 북마크(`lightImpact`), 재생(`mediumImpact`), 다이얼 탭/드래그(`selectionClick`) 등 터치 피드백을 인터랙션 강도별로 세분화했습니다.
*   **개인정보처리방침 구축**: 다국어(영/한/중) `PRIVACY.md`와 상용 호스팅이 가능한 `docs/privacy.html` 배포를 마쳤습니다. (호스팅 URL: `https://legitstudio.cc/moodtube/privacy`)
*   **R8/난독화 최적화**: `minifyEnabled` 및 `shrinkResources`를 켜고 AAB 빌드 시 Play Console 오류 추적을 위한 디버그 심볼 테이블(`SYMBOL_TABLE`)을 포함하도록 Gradle 설정을 완료했습니다.

---

## [0.1.0] - 2026-06-03 (최초 알파 빌드)
### 추가 및 변경 사항
*   **무드 스펙트럼 다이얼**: 감정 다이얼 게이지를 돌려 에너지를 측정하고 기분에 어울리는 음악을 분석하는 큐레이션 엔진을 구현했습니다.
*   **임베드 유튜브 플레이어**: `youtube_player_flutter` 기반의 단일 오디오 재생 플레이어 및 미니플레이어를 결합했습니다.
*   **YouTube Data API 연동**: 유튜브 검색 API 통신 및 통신 실패 시 내부 mock 카탈로그로 즉시 안전하게 fallback하는 기능을 탑재했습니다.
*   **3개 국어 다국어 및 시스템 테마 연동**: 영어, 한국어, 중국어(간체) 번역 시스템 구축 및 시스템 다크/라이트 테마 전환 영속 기능을 최초로 안착시켰습니다.
